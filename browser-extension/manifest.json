{
  "manifest_version": 3,
  "name": "Active Tab Query Extension",
  "version": "1.0",
  "description": "Allows node process to query the browser's active tab and URL if browser is focused.",
  "permissions": ["activeTab", "tabs", "scripting"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html"
  }
}
